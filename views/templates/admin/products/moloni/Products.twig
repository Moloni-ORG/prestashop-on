{% extends view_dir ~ 'Layout.twig' %}

{% block plugin_content %}
    <div class="card">
        <h3 class="card-header">
            <i class="material-icons  mi-logo">logo</i>
            {{ 'Moloni ON products'|trans({}, 'Modules.Molonion.Common') }}
        </h3>
        <div class="card-block overflow-auto">
            <table class="table table-striped table-bordered">
                {# Table header #}
                {% include view_dir ~ 'products/moloni/blocks/TableHeader.twig' %}

                {# Table body #}
                {% include view_dir ~ 'products/moloni/blocks/TableBody.twig' %}
            </table>

            {# Paginator #}
            {% include view_dir ~ 'components/Paginator.twig' %}
        </div>

        <div class="card-footer">
            <div class="d-flex justify-content-end">
                {# Go to tools #}
                <a href="{{ path(toolsRoute) }}"
                   class="btn btn-primary mr-2">
                    {{ 'Go back'|trans({}, 'Modules.Molonion.Common') }}
                </a>

                {# Refresh list #}
                <a href="{{ path(thisRoute, {page: paginator.currentPage|default(1), filters: filters}) }}"
                   class="btn btn-outline-secondary">
                    {{ 'Refresh'|trans({}, 'Modules.Molonion.Common') }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function () {
            window.molonion.MoloniProducts.startObservers({
                thisRoute: "{{ path(thisRoute) }}",
                ImportStockRoute: "{{ path(importStockRoute) }}",
                ImportProductRoute: "{{ path(importProductRoute) }}"
            });
        });
    </script>
{% endblock %}
