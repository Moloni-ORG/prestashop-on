{% extends view_dir ~ 'Layout.twig' %}

{% block plugin_content %}
    <div class="card">
        <h3 class="card-header">
            <i class="material-icons mi-store">store</i>
            {{ 'PrestaShop products'|trans({}, 'Modules.Molonion.Common') }}
        </h3>
        <div class="card-block overflow-auto">
            <table class="table table-striped table-bordered">
                {# Table header #}
                {% include view_dir ~ 'products/prestashop/blocks/TableHeader.twig' %}

                {# Table body #}
                {% include view_dir ~ 'products/prestashop/blocks/TableBody.twig' %}
            </table>

            {# Paginator #}
            {% include view_dir ~ 'components/Paginator.twig' %}
        </div>

        <div class="card-footer">
            <div class="d-flex justify-content-end">
                {# Go to tools #}
                <a href="{{ path(toolsRoute) }}"
                   class="btn btn-primary mr-2">
                    {{ 'Go back'|trans({}, 'Modules.Molonion.Common') }}
                </a>

                {# Refresh list #}
                <a href="{{ path(thisRoute, {page: paginator.currentPage, filters: filters}) }}"
                   class="btn btn-outline-secondary">
                    {{ 'Refresh'|trans({}, 'Modules.Molonion.Common') }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function () {
            window.molonion.PrestashopProducts.startObservers({
                thisRoute: "{{ path(thisRoute) }}",
                ExportStockRoute: "{{ path(exportStockRoute) }}",
                ExportProductRoute: "{{ path(exportProductRoute) }}"
            });
        });
    </script>
{% endblock %}
