<div class="modal fade"
     id="export_stocks_modal"
     tabindex="-1"
     role="dialog"
     aria-hidden="true">

    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    {{ 'Update Moloni ON stocks'|trans({}, 'Modules.Molonion.Tools') }}
                </h5>
                <button type="button"
                        class="close"
                        data-dismiss="modal"
                        aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>
                    {{ 'This tool will cycle for all your PrestaShop products and will insert manual stock movements in your Moloni ON account to make sure the stocks are equal in both platforms.'|trans({}, 'Modules.Molonion.Tools') }}<br>
                    {{ 'When the tool is finish, all your Moloni ON stock will be updated to match the stock on your PrestaShop store.'|trans({}, 'Modules.Molonion.Tools') }}
                    {{ 'This may take a while, so, please keep this window open until the process finishes.'|trans({}, 'Modules.Molonion.Tools') }}
                </p>
                <p>
                    {{ 'Are you sure you want to continue?'|trans({}, 'Modules.Molonion.Tools') }}
                </p>
            </div>
            <div class="modal-footer">
                <button type="button"
                        class="btn btn-outline-secondary"
                        data-dismiss="modal">
                    {{ 'Close'|trans({}, 'Modules.Molonion.Tools') }}
                </button>
                <button class="btn btn-primary"
                        data-dismiss="modal"
                        id="export_stocks_button">
                    {{ 'Export stocks'|trans({}, 'Modules.Molonion.Tools') }}
                </button>
            </div>
        </div>
    </div>
</div>
