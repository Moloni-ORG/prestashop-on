query prestashopEstimateGetPDFToken($documentId: Int!) {
  estimateGetPDFToken(documentId: $documentId) {
    data {
      token
      filename
      path
    }
    errors {
      field
      msg
    }
  }
}

