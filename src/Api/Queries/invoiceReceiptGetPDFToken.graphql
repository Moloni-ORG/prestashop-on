query prestashopInvoiceReceiptGetPDFToken($documentId: Int!) {
  invoiceReceiptGetPDFToken(documentId: $documentId) {
    data {
      token
      filename
      path
    }
    errors {
      field
      msg
    }
  }
}

